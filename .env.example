# Example environment variables for development
# Copy this to .env and update values as needed

# URL where your API is reachable from inside the container (host gateway)
API_BASE_URL=http://172.24.0.1:8004

# Django debug mode
DEBUG=1

# Any other variables your project requires (DB, secret key, etc.)
# SECRET_KEY=changeme
# DATABASE_URL=postgres://user:pass@db:5432/dbname
